#include <bits/stdc++.h>

using namespace std;

int main (void)
{
    int N = 0;
    cin >> N;

    vector<int> lista;
    for (int i = 0; i < N; i++)
    {
        int aux;
        cin >> aux;
        lista.push_back(aux);
    }
    unordered_map<int, int> mapeie;
}